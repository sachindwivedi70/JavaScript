//stop at first multiple of 7 
// write a loop from 1 to 100 that:
// prints each number
// stops completely when if finds the first number divisivle by 7

for ( let i=1;i<101;i++){
    if(i%7==0 ){
      break;
        
    }
    console.log(i);
}